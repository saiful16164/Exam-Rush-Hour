{
  "name": "exam_rush_hour",
  "version": "1.0.0",
  "scripts": {
    "build": "if [ -d \"flutter\" ]; then echo \"Flutter already installed\"; else git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter build web --release && rm -rf web && mv build/web web"
  }
}
